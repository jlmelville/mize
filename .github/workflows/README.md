# Workflow Validation

Run these local checks after editing GitHub Actions workflows:

- `actionlint`
- `zizmor .github/workflows`

If `zizmor` is not installed locally, use `uvx zizmor .github/workflows`.
