% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradient_check.R
\name{check_mize_gradient}
\alias{check_mize_gradient}
\title{Check Gradient Consistency}
\usage{
check_mize_gradient(
  fg,
  par,
  method = c("central", "forward"),
  rel_eps = NULL,
  abs_eps = 0,
  ...
)
}
\arguments{
\item{fg}{Function and gradient list. See the documentation of \code{\link[=mize]{mize()}}.}

\item{par}{Parameter vector where the gradient should be checked.}

\item{method}{Finite-difference method: either \code{"central"} or \code{"forward"}.}

\item{rel_eps}{Relative finite-difference step. If \code{NULL}, a method-specific
default is used.}

\item{abs_eps}{Non-negative scalar added to each finite-difference step.}

\item{...}{Additional arguments passed to \code{fg$fn}, \code{fg$gr}, and \code{fg$fg} when
present.}
}
\value{
A list with components:
\itemize{
\item \code{par}: Parameter vector checked.
\item \code{method}: Finite-difference method used.
\item \code{step}: Per-coordinate finite-difference steps.
\item \code{fn}: Function value at \code{par}.
\item \code{gr}: Gradient returned by \code{fg$gr(par)}.
\item \code{gr_fd}: Finite-difference gradient approximation.
\item \code{error}: Difference \code{gr - gr_fd}.
\item \code{abs_error}: Absolute coordinate-wise errors.
\item \code{rel_error}: Relative coordinate-wise errors, scaled by the larger
coordinate magnitude of \code{gr} and \code{gr_fd}.
\item \code{max_abs_error}: Maximum absolute error.
\item \code{max_rel_error}: Maximum relative error.
\item \code{worst_abs}: One-row data frame for the coordinate with the largest
absolute error.
\item \code{worst_rel}: One-row data frame for the coordinate with the largest
relative error.
\item \code{by_coord}: Data frame with per-coordinate gradient comparisons.
\item \code{fg_consistency}: List describing whether \code{fg$fg()} was checked and, when
available, its function and gradient consistency with \code{fg$fn()} and
\code{fg$gr()}.
}
}
\description{
Compares an analytic gradient with a finite-difference approximation of the
objective function at a parameter vector.
}
\details{
The \code{fg} argument uses the same list shape as \code{\link[=mize]{mize()}}: it must contain
\code{fn} and \code{gr} functions. If it also contains an \code{fg} function, the
combined function-and-gradient result is checked for consistency with the
separate \code{fn} and \code{gr} functions at \code{par}.

The finite-difference step for each coordinate is
\code{abs_eps + rel_eps * pmax(abs(par), 1)}. Central differences are usually
more accurate and use twice as many function evaluations as forward
differences.
}
\examples{
rb_fg <- list(
  fn = function(x) {
    100 * (x[2] - x[1] * x[1])^2 + (1 - x[1])^2
  },
  gr = function(x) {
    c(
      -400 * x[1] * (x[2] - x[1] * x[1]) - 2 * (1 - x[1]),
      200 * (x[2] - x[1] * x[1])
    )
  }
)

check <- check_mize_gradient(rb_fg, c(-1.2, 1))
check$max_abs_error
}
\seealso{
\code{\link[=mize]{mize()}}
}
